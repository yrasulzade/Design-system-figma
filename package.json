{
  "name": "style-dictionary-custom-transforms",
  "version": "1.0.0",
  "description": "",
  "files": [
    "build",
    "properties"
  ],
  "scripts": {
    "build": "rm -rf build && token-transformer ./tokens/tokens.json ./tokens/hds-token-transformer.json --resolveReferences=false && node ./build.js",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "style-dictionary": "3.7.2",
    "token-transformer": "^0.0.29"
  }
}
